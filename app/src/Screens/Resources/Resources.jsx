import React, { useState, eseEffect } from "react";
import api from "../../api/api";
import { ResourcesPresentation } from "./ResourcesPresentation";

export const Resources = () => {
  return <ResourcesPresentation />;
};
